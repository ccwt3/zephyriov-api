# Informe diario T02 — herramientas mínimas de API

- **Fecha / repositorio / revisión:** 2026-09-18, Mexico/General;
  `zephyriov-api`, HEAD `3bf55a4727c53848926a37fb0f179f03d668e183`.
- **Punto / objetivo:** T02, después de T01; disponer de un paquete TypeScript
  mínimo con pnpm, Vitest, lint, typecheck, build y lockfile. Sin dominio SRS,
  contratos, servidor, proveedores ni clientes.
- **Archivos modificados:** `package.json`, `pnpm-lock.yaml`,
  `pnpm-workspace.yaml`, `tsconfig.json`, `eslint.config.mjs`, `.gitignore`,
  `src/index.ts`, `src/index.test.ts`, este informe y resúmenes README,
  Branch_changes, docs/next-job.
- **Cambios realizados:** export público `SRS_SPECIFICATION_VERSION`,
  ligado a `T01-transfer.json`; paquete ESM privado `zephyriov-api@0.0.0`.
  Node 24 y pnpm 12.4.2 son objetivos fijados; nombre publicable final y
  distribución quedan para B11.05. Dependencias exactas: TypeScript 5.9.3,
  Vitest 5.0.1, ESLint 10.11.0, typescript-eslint 8.70.0 y tipos Node
  24.13.6. Se eligió TS 5.9 porque el peer de typescript-eslint 8.70 exige
  TypeScript menor que 6.1; TS 7.0.2 no cumple. El archivo workspace conserva
  las dos excepciones de antigüedad que pnpm añadió al instalar versiones
  exactas recientes.
- **Pruebas ejecutadas:** `pnpm install` generó lockfile e instaló 130
  paquetes; `pnpm test`: 1/1 prueba aprobada y verificación del lockfile.
  `./node_modules/.bin/eslint .`, `./node_modules/.bin/tsc -p tsconfig.json
  --noEmit` y `./node_modules/.bin/tsc -p tsconfig.json`: aprobados.
  `npm pack --pack-destination /tmp/zephyriov-t02/final --json` creó un
  tarball local; un consumidor temporal extrajo el artefacto en `node_modules`
  e importó `zephyriov-api` por nombre: devolvió `B01.10-v1`. El artefacto
  incluye `dist/index.js`, tipos, package.json y README.
- **Entorno / resultado:** Linux, Node 26.9.0 disponible, pnpm 12.4.2,
  TypeScript 5.9.3, Vitest 5.0.1; pruebas, lint, typecheck, build y consumidor
  aprobados, cero fallos de esos controles. La ejecución específica bajo
  Node 24 no se completó: la instalación temporal `node@24.21.0` bloqueó su
  script de instalación por política local. No es una prueba de compatibilidad
  de runtime Node 24; comprobarla al preparar B03/B04.
- **Evidencia:** SHA-256 de `pnpm-lock.yaml`
  `ebb0fdbcfa8ae2075d2a44a0a390955367950728d21fe667ef6305e39652f993`;
  tarball temporal de la prueba final
  `41d0ca9a8dd78dd444ae448388a060182029164a49bb4b306c3b70ae469ee6d7`.
  Sin commit nuevo. El tarball es ensayo local, no una versión publicada.
- **Estado:** `construido_local`, `probado(Node 26.9.0, Linux)`. Sin
  `desplegado` ni `aprobado_desarrollador` atribuidos.
- **Documentación / manual / decisiones:** este informe, README,
  Branch_changes, docs/next-job. Ninguna credencial ni intervención manual
  para T02. Compatibilidad completa del stack y matriz Node 24 permanecen
  en B04; propietario y paquete publicable en B11.05.
- **Riesgos / siguiente:** el test cubre correspondencia de versión y el
  consumidor cubre exportación del artefacto, no funciones de dominio.
  Siguiente punto **B02.01**, primitivas, IDs, fechas, revisiones y sobre de
  error; no se ejecutó aquí.

Fuentes técnicas consultadas: [Vitest 5](https://vitest.dev/guide/),
[TypeScript 5.9](https://www.typescriptlang.org/docs/handbook/release-notes/typescript-5-9.html),
[typescript-eslint](https://typescript-eslint.io/getting-started/) y
[ESLint](https://eslint.org/docs/latest/use/getting-started).
