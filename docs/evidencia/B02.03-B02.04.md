# Informe diario B02.03–B02.04

- **Fecha / repositorio / revisión:** 2026-09-18, Mexico/General;
  `zephyriov-api`, HEAD de entrada `b10846d3b3343eb068115f36e77655f63c396046`.
  El trabajo B02.01–B02.02 ya estaba sin commit al entrar; se conservó.
- **Punto trabajado:** B02.03 y después B02.04.
- **Objetivo:** contrato local verificable para perfil, ajustes y repertorio
  versionados; sesión/ítems con snapshots y origen estable; solicitud y
  respuesta de onboarding con requisitos de selección/atomicidad expresados.
- **Archivos modificados:** `contracts/account.mjs`, `contracts/study.mjs`,
  `contracts/index.mjs`, `contracts/examples-B02.03-B02.04.json`,
  `contracts/account-study.test.mjs`, `package.json`,
  `docs/contrato-B02.03-B02.04.md`, este informe, `docs/next-job`, README y
  Branch_changes. `pnpm-lock.yaml` y los archivos B02.01–B02.02 ya estaban
  modificados al entrar; no se editaron para estos puntos. Los fixtures SRS
  y la referencia permanecen intactos.
- **Cambios realizados:** esquemas Zod estrictos para Settings, Profile,
  RepertoireEntry, Card, StudyItem, StudySession, Activity y AccountState;
  solicitudes/respuestas de PUT/DELETE/PATCH/onboarding/sesión. Validación
  cruzada del color permitido, `If-Match`, selección inicial no vacía,
  versiones decimales, conteos, revisión de líneas, reintento con origen
  conservado y respuesta de onboarding sin selecciones parciales. Helpers
  puros distinguen aplicar, repetir estado deseado y
  conflicto real. El paquete exporta los módulos y ejemplos nuevos.
- **Pruebas ejecutadas (comandos, entorno, casos):** `pnpm test` aprobó 15/15
  en tres archivos. Cubre Settings `"1"`→`"2"`, `onboardedAt:null`, rangos
  y zona inválidos, color no permitido, precondiciones `"0"`/`If-Match`,
  repetición sin efecto y 409 lógico, snapshot pedagógico viejo tras ajuste,
  origen `new` en reintento, parent ID, conteos y selección de onboarding
  vacía/duplicada/incompatible o ausente de la respuesta. `pnpm lint`,
  `pnpm typecheck`, `pnpm build`
  aprobaron. `python3 docs/evidencia/verificar-T01.py` aprobó: 22 JSON
  idénticos, 352 IDs únicos. `git diff --check` aprobó. `npm pack
  --ignore-scripts --pack-destination /tmp --json`, instalación con `pnpm
  install --offline` y `node check.mjs` en consumidor temporal aprobaron
  importación por nombre y validación de los tres ejemplos principales.
  Linux, Node 26.9.0; pnpm 12.4.2 en proyecto y 12.0.0-rc.5 en consumidor.
- **Resultado:** B02.03 y B02.04 `construido_local`, `probado(Node 26.9.0,
  Linux)`; cero fallos finales. La primera prueba detectó que una apertura
  completa del catálogo excedía el esquema de resumen; se corrigió y se
  repitieron las pruebas. `pnpm pack` no produjo salida y se interrumpió;
  `npm pack` creó y verificó el artefacto equivalente. Sin despliegue ni
  aprobación del desarrollador.
- **Evidencia:** prueba y ejemplos bajo `contracts/`; [contrato](../contrato-B02.03-B02.04.md).
  SHA-256 del tarball local `zephyriov-api-0.0.0.tgz`:
  `d3b8a62c0fb6959e96df04b3b5b2b3338810e3f38f90ff63120f955c1a4f0c59`.
  `git status --short` mostró únicamente los archivos B02.01–B02.04 y
  documentación declarados; no hay commit nuevo.
- **Estado:** `construido_local`, `probado(Node 26.9.0, Linux)`.
- **Documentación actualizada:** contrato, este informe, README,
  Branch_changes y `docs/next-job`.
- **Trabajo manual pendiente:** ninguno para estos puntos. Propietario y
  publicación del paquete corresponden a B11.05.
- **Decisiones pendientes:** ninguna bloquea B02.05. Rutas Auth efectivas
  B05.12, DTO/OpenAPI B02.09, detalle de Activity B02.10/B08.08.
- **Riesgos y limitaciones:** Node 24 objetivo no probado. Los esquemas y
  helpers no demuestran transacción de onboarding, unicidad de sesión,
  autorización HTTP ni persistencia; esas garantías esperan B04/B08. El
  consumidor verifica instalación local bajo Node 26, no runtime Android/web.
  El `typecheck`/build actual cubre `src/`; los `.mjs` del contrato se
  comprueban por lint, Vitest y consumidor hasta generar DTO en B02.09.
- **Siguiente punto elegible y dependencias:** B02.05, StudyEvent inmutable,
  intentos crudos, base y dependencias; B02.01–B02.04 locales disponibles.
  No se ejecutó B02.05 en esta sesión.
